<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Climate Solutions - <%= project.title %></title>
  <link href="/css/main.css" rel="stylesheet">
</head>
<body class="bg-base-100">
  <%- include('partials/navbar', { page: page }) %>
  <div class="container mx-auto mt-8">
    <div class="hero bg-base-200 rounded-lg p-8">
      <div class="hero-content text-center">
        <div class="max-w-md">
          <h1 class="text-4xl font-bold"><%= project.title %></h1>
          <p class="py-6">Below, you will find detailed information about the project.</p>
        </div>
      </div>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-8">
      <div>
        <img src="<%= project.feature_img_url %>" alt="<%= project.title %>" class="rounded-lg w-full">
      </div>
      <div class="prose">
        <h2>Introduction</h2>
        <p><%= project.intro_short %></p>
        <h2>Impact</h2>
        <p><%= project.impact %></p>
        <a href="<%= project.original_source_url %>" class="btn btn-primary" target="_blank">Learn More</a>
      </div>
    </div>
    <div class="mt-8">
      <blockquote class="border-l-4 border-primary pl-4">
        <p id="quote" class="italic">Loading quote...</p>
        <p id="author" class="text-sm mt-2"></p>
      </blockquote>
    </div>
    <div class="mt-8">
      <a href="#" onclick="history.back(); return false;" class="btn btn-outline">Back</a>
    </div>
  </div>
  <script>
    document.addEventListener("DOMContentLoaded", () => {
      fetch("https://dummyjson.com/quotes/random")
        .then(res => res.json())
        .then(data => {
          document.getElementById("quote").innerText = data.quote;
          document.getElementById("author").innerText = "- " + data.author;
        });
    });
  </script>
</body>
</html>